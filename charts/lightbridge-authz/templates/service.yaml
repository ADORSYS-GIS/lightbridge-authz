apiVersion: v1
kind: Service
metadata:
  name: {{ include "lightbridge-authz.fullname" . }}
  labels:
    {{- include "lightbridge-authz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lightbridge-authz.selectorLabels" . | nindent 4 }}
