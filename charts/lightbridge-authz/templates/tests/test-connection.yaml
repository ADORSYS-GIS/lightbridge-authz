apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lightbridge-authz.fullname" . }}-test-connection"
  labels:
    {{- include "lightbridge-authz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lightbridge-authz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
