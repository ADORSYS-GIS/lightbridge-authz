//! Shared public API types and traits across REST and gRPC.

/// API contracts shared between REST and gRPC.
pub mod contract {
    /// Placeholder type to ensure the crate links and can be referenced by dependents.
    pub struct Placeholder;
}
