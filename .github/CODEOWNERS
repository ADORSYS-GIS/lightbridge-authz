* @stephane-segning

# A grandfathered-ceiling raise (an entry going up, or a new key appearing in the
# baseline file) must carry the `loc-baseline-raise` PR label enforced by the
# loc-gate; this rule makes the same change require a maintainer's review before
# merge. Lowering or removing entries stays always-free.
/.github/loc-baseline.json @stephane-segning
