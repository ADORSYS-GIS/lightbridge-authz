name: CI/CD Pipeline
on:
  push: {}
  pull_request: {}

jobs:
  sast:
    name: SAST Scan
    runs-on: adorsys-gis-runner
    strategy:
      matrix:
        checks: [advisories,"bans licenses sources"]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - uses: ./.github/actions/cargo-deny
        with:
          check: ${{ matrix.checks }}

  code-checks:
    runs-on: adorsys-gis-runner
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - uses: ./.github/actions/code-checks

  test:
    needs: code-checks
    runs-on: adorsys-gis-runner
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - uses: ./.github/actions/tests
        with:
          database-url: postgres://postgres:postgres@localhost:5432/lightbridge_authz

  binaries:
    needs: code-checks
    runs-on: adorsys-gis-runner
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - uses: ./.github/actions/build-release

  integration-test:
    needs: [code-checks, binaries]
    runs-on: adorsys-gis-runner
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - name: Download prebuilt binaries
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131
        with:
          name: dist-binaries
          path: dist
      - uses: ./.github/actions/integration-test

  container-build:
    needs: [binaries, test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: adorsys-gis-runner
    strategy:
      matrix:
        include:
          - { target: runtime, image_name: '${{ github.repository }}' }
          - { target: mcp-runtime, image_name: '${{ github.repository }}-mcp' }
          - { target: usage-runtime, image_name: '${{ github.repository }}-usage' }
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - name: Build and push container image
        id: build-image
        uses: ./.github/actions/container-build
        with:
          app-version: ${{ github.sha }}
          image-name: ${{ matrix.image_name }}
          target: ${{ matrix.target }}
          platform: linux/amd64
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.36.0
        env:
          TRIVY_IMAGE_SRC: remote
          TRIVY_PLATFORM: linux/amd64
        with:
          image-ref: ${{ steps.build-image.outputs.version-tag }}
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'HIGH,CRITICAL'
  release:
    needs: [binaries,container-build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: adorsys-gis-runner
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - uses: ./.github/actions/release
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
